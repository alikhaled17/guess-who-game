{
  "name": "guess-who-signaling-server",
  "version": "2.0.0",
  "private": true,
  "description": "Minimal WebSocket signaling relay used ONLY to bootstrap WebRTC handshakes between two peers, deployed as a Cloudflare Worker + Durable Object. Carries no gameplay data. See ../signaling-server/legacy-node-server for the original plain-Node implementation.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.99.0",
    "ws": "^8.18.0"
  }
}
