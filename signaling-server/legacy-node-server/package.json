{
  "name": "guess-who-signaling-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal WebSocket signaling server used ONLY to bootstrap WebRTC handshakes between two peers. Carries no gameplay data.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
